{
  "buildCommand": "flutter build web --release",
  "outputDirectory": "build/web",
  "env": {
    "FLUTTER_WEB": "true"
  },
  "regions": ["sfo1"],
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}